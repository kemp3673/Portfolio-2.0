import * as dotenv from "dotenv";
import axios from "axios";

dotenv.config();

//const endpoint = process.env.CONTACTENDPOINT;
const endpoint = 'test';

const sendContact = async () => {
  const query = `${endpoint}`;
  }

  export default sendContact;